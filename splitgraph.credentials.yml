# Normally, we'd put this file into GitHub Secrets, but in this case,
# we don't need any API keys to access Socrata, so we keep it here.
credentials:
  dbt-sample:
    plugin: dbt
    data:
      git_url: $THIS_REPO_URL
